import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';
import GiftExpertApp from './GiftExpertApp';

ReactDOM.render(
  <React.StrictMode>
    <App />
    <hr></hr>
    <GiftExpertApp></GiftExpertApp>
  </React.StrictMode>,
  document.getElementById('root')
);

// https://animate.style/ para estilos de css
